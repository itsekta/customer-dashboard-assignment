# customer-dashboard-assignment

### Steps to run the project
- Start the docker container by the following command
	`docker-compose up -d`

- In the new terminal start the express app by running the following command
	`npm run dev`
